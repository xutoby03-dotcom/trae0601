## 1. 架构设计
```mermaid
graph TB
    A["前端 React + TypeScript"] --> B["状态管理 Zustand"]
    B --> C["本地存储 LocalStorage"]
    A --> D["UI 组件库 TailwindCSS"]
    A --> E["路由 React Router"]
```

## 2. 技术描述
- 前端：React@18 + TypeScript + Vite
- 样式：TailwindCSS@3
- 状态管理：Zustand
- 路由：React Router DOM@6
- 图表：纯 CSS 实现柱状图
- 数据存储：LocalStorage（模拟后端，纯前端应用）
- 初始化工具：Vite

## 3. 路由定义
| 路由 | 用途 |
|------|------|
| / | 首页，展示分组组队列表 |
| /create | 创建新组队 |
| /team/:id | 组队详情与报名 |
| /team/:id/review | 体验记录 |
| /stats | 统计页面 |

## 4. 数据模型

### 4.1 数据模型定义
```mermaid
erDiagram
    TEAM ||--o{ MEMBER : has
    TEAM ||--o| REVIEW : has
    TEAM {
        string id
        string shopName
        string themeName
        string themeType
        number totalPeople
        number price
        number duration
        string difficulty
        string horrorLevel
        string[] availableTimes
        string status
        Date createdAt
    }
    MEMBER {
        string id
        string name
        boolean acceptHorror
        boolean motionSickness
        string skill
        number budgetLimit
        Date joinedAt
    }
    REVIEW {
        string id
        string teamId
        number rating
        string bestPuzzleSolver
        boolean hiddenCost
        string notes
        Date createdAt
    }
```

### 4.2 TypeScript 类型定义
```typescript
type ThemeType = 'horror' | 'mystery' | 'mechanism';
type Difficulty = 'easy' | 'medium' | 'hard' | 'expert';
type HorrorLevel = 'none' | 'mild' | 'medium' | 'high' | 'extreme';
type Skill = 'puzzle' | 'search' | 'both';
type TeamStatus = 'recruiting' | 'locked' | 'completed';

interface Member {
  id: string;
  name: string;
  acceptHorror: boolean;
  motionSickness: boolean;
  skill: Skill;
  budgetLimit: number;
  joinedAt: string;
}

interface Review {
  id: string;
  teamId: string;
  rating: number;
  bestPuzzleSolver: string;
  hiddenCost: boolean;
  hiddenCostAmount?: number;
  notes: string;
  createdAt: string;
}

interface Team {
  id: string;
  shopName: string;
  themeName: string;
  themeType: ThemeType;
  totalPeople: number;
  price: number;
  duration: number;
  difficulty: Difficulty;
  horrorLevel: HorrorLevel;
  availableTimes: string[];
  status: TeamStatus;
  members: Member[];
  review?: Review;
  createdAt: string;
}
```
