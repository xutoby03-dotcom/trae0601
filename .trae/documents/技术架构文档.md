## 1. 架构设计

```mermaid
graph TB
    subgraph "前端层"
        A["React 应用"]
        A1["数据看板页面"]
        A2["设备档案页面"]
        A3["巡检记录页面"]
        A4["整改管理页面"]
        A --> A1 & A2 & A3 & A4
    end
    
    subgraph "状态管理层"
        B["Zustand Store"]
        B1["设备状态"]
        B2["巡检状态"]
        B3["整改单状态"]
        B --> B1 & B2 & B3
    end
    
    subgraph "数据层"
        C["Mock 数据"]
        C1["设备数据"]
        C2["巡检数据"]
        C3["整改单数据"]
        C --> C1 & C2 & C3
    end
    
    subgraph "UI组件层"
        D["通用组件"]
        D1["统计卡片"]
        D2["数据表格"]
        D3["表单组件"]
        D4["模态框"]
        D --> D1 & D2 & D3 & D4
    end
    
    A --> B
    B --> C
    A --> D
```

## 2. 技术描述

- **前端框架**：React@18 + TypeScript
- **构建工具**：Vite
- **样式方案**：Tailwind CSS@3
- **状态管理**：Zustand
- **路由管理**：React Router DOM
- **图标库**：Lucide React
- **数据方案**：前端 Mock 数据（无后端）

## 3. 路由定义

| 路由路径 | 页面名称 | 说明 |
|----------|----------|------|
| /dashboard | 数据看板 | 系统首页，展示统计数据和概览 |
| /devices | 设备档案 | 设备列表和管理 |
| /devices/:id | 设备详情 | 单台设备详细信息 |
| /inspections | 巡检记录 | 巡检记录列表 |
| /inspections/new | 新建巡检 | 新增巡检记录 |
| /rectifications | 整改管理 | 整改单列表和管理 |
| /rectifications/:id | 整改详情 | 整改单详情和处理 |

## 4. 数据模型

### 4.1 数据模型定义

```mermaid
erDiagram
    DEVICE ||--o{ INSPECTION : "has"
    DEVICE ||--o{ RECTIFICATION : "may have"
    INSPECTION ||--o| RECTIFICATION : "may generate"
    
    DEVICE {
        string id "设备ID"
        string code "设备编号"
        string building "楼栋"
        string floor "楼层"
        string location "位置"
        string type "类型"
        number minPressure "最小压力"
        number maxPressure "最大压力"
        string expireDate "有效期"
        string photo "照片URL"
        string qrCode "二维码"
        string status "状态"
        date createdAt "创建时间"
    }
    
    INSPECTION {
        string id "巡检ID"
        string deviceId "设备ID"
        date inspectDate "巡检日期"
        string inspector "检查人"
        number pressure "压力值"
        string pressureStatus "压力状态"
        boolean seal "铅封完好"
        boolean hose "喷管完好"
        boolean boxDoor "箱门完好"
        boolean obstruction "有遮挡物"
        string result "巡检结果"
        string remark "备注"
    }
    
    RECTIFICATION {
        string id "整改单ID"
        string deviceId "设备ID"
        string inspectionId "关联巡检ID"
        string type "整改类型"
        string description "问题描述"
        string status "状态"
        date createDate "创建时间"
        date deadline "截止日期"
        string handler "处理人"
        string fixPhoto "整改照片"
        date fixDate "整改完成时间"
        string fixRemark "整改说明"
    }
```

### 4.2 类型定义

```typescript
// 设备类型
type DeviceType = '干粉灭火器' | '二氧化碳灭火器' | '泡沫灭火器' | '水基灭火器';

interface Device {
  id: string;
  code: string;
  building: string;
  floor: string;
  location: string;
  type: DeviceType;
  minPressure: number;
  maxPressure: number;
  expireDate: string;
  photo: string;
  status: 'normal' | 'warning' | 'danger';
  createdAt: string;
}

// 巡检记录
type PressureStatus = 'normal' | 'low' | 'high';
type InspectionResult = 'normal' | 'abnormal';

interface Inspection {
  id: string;
  deviceId: string;
  inspectDate: string;
  inspector: string;
  pressure: number;
  pressureStatus: PressureStatus;
  seal: boolean;
  hose: boolean;
  boxDoor: boolean;
  obstruction: boolean;
  result: InspectionResult;
  remark: string;
}

// 整改单
type RectificationType = 'pressure' | 'expired' | 'obstruction' | 'other';
type RectificationStatus = 'pending' | 'processing' | 'closed';

interface Rectification {
  id: string;
  deviceId: string;
  inspectionId: string;
  type: RectificationType;
  description: string;
  status: RectificationStatus;
  createDate: string;
  deadline: string;
  handler: string;
  fixPhoto?: string;
  fixDate?: string;
  fixRemark?: string;
}

// 看板统计
interface DashboardStats {
  totalDevices: number;
  passRate: number;
  expiringSoon: number;
  pendingRectifications: number;
  buildingStats: BuildingStat[];
  repeatAnomalies: AnomalyPoint[];
  uncheckedThisMonth: Device[];
}

interface BuildingStat {
  building: string;
  total: number;
  inspected: number;
  passRate: number;
}

interface AnomalyPoint {
  deviceId: string;
  deviceCode: string;
  location: string;
  anomalyCount: number;
  lastAnomalyDate: string;
}
```

## 5. 项目结构

```
src/
├── components/          # 通用组件
│   ├── Layout/         # 布局组件
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   └── index.tsx
│   ├── StatCard.tsx    # 统计卡片
│   ├── DataTable.tsx   # 数据表格
│   └── StatusBadge.tsx # 状态标签
├── pages/              # 页面组件
│   ├── Dashboard/      # 数据看板
│   ├── Devices/        # 设备档案
│   ├── Inspections/    # 巡检记录
│   └── Rectifications/ # 整改管理
├── store/              # 状态管理
│   ├── deviceStore.ts
│   ├── inspectionStore.ts
│   └── rectificationStore.ts
├── data/               # Mock数据
│   ├── devices.ts
│   ├── inspections.ts
│   └── rectifications.ts
├── types/              # 类型定义
│   └── index.ts
├── utils/              # 工具函数
│   ├── date.ts
│   └── calculation.ts
├── App.tsx
├── main.tsx
└── index.css
```
