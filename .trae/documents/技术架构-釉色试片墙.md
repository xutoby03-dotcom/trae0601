## 1. 架构设计

```mermaid
graph TD
    A["釉色试片墙前端应用"] --> B["React 18 视图层"]
    B --> B1["试片墙首页（筛选+网格）"]
    B --> B2["试片详情弹窗组件"]
    B --> B3["演变链路时间轴组件"]
    A --> C["状态管理层（React Context + useReducer）"]
    C --> C1["筛选条件状态"]
    C --> C2["当前选中试片"]
    C --> C3["演变链路数据"]
    A --> D["数据层（Mock Data + TypeScript）"]
    D --> D1["试片数据 TS 类型定义"]
    D --> D2["配方演变链路数据"]
    A --> E["样式层（TailwindCSS 3 + 自定义 CSS）"]
    E --> E1["陶艺工作室主题变量"]
    E --> E2["动效与微交互"]
```

## 2. 技术描述

- **前端**：React@18 + TypeScript@5 + TailwindCSS@3 + Vite@5
- **初始化工具**：Vite（react-ts 模板）
- **后端**：无（纯前端应用，使用本地 Mock 数据）
- **数据存储**：TypeScript 类型定义的静态 Mock 数据
- **动画库**：Framer Motion（用于卡片动效、弹窗过渡、时间轴动画）
- **图标库**：Lucide React

## 3. 路由定义

| Route | 用途 |
|-------|------|
| / | 试片墙首页（所有功能集中于单页应用，通过弹窗和滚动区域切换视图） |

## 4. 数据模型

### 4.1 数据模型定义

```mermaid
erDiagram
    TEST_SAMPLE ||--o{ GLAZE_RECIPE : "使用配方"
    TEST_SAMPLE ||--o{ EVOLUTION_CHAIN : "属于演变链路"
    TEST_SAMPLE {
        string id PK "试片编号"
        string name "试片名称"
        string color_family "色系"
        string hex_color "主色HEX值"
        string front_image "正面照片URL"
        string back_image "背面照片URL"
        number firing_temperature "烧成温度(℃)"
        string atmosphere "烧成气氛：氧化/还原"
        string clay_type "泥料类型"
        string glaze_name "釉料名称"
        string kiln_position "窑位"
        string firing_date "烧成日期"
        number flow_grade "流釉程度 0-5"
        number pinhole_grade "针孔程度 0-5"
        number crazing_grade "开片程度 0-5"
        string glossiness "光泽度"
        string touch_feel "触感描述"
        string recipe_id FK "配方ID"
        string evolution_chain_id FK "演变链路ID"
        number version "配方版本号"
        string previous_version_id "上一版试片ID"
        string notes "备注"
    }
    GLAZE_RECIPE {
        string id PK "配方ID"
        string name "配方名称"
        object ingredients "成分及百分比"
        string notes "配方备注"
    }
    EVOLUTION_CHAIN {
        string id PK "链路ID"
        string name "链路名称（如：青瓷系列）"
        string[] sample_ids "试片ID序列"
        string description "演变描述"
    }
```

### 4.2 TypeScript 类型定义

```typescript
// 试片表面效果等级（0-5，0=无，5=极严重）
type Grade = 0 | 1 | 2 | 3 | 4 | 5;

// 烧成气氛
type Atmosphere = '氧化' | '还原';

// 色系分类
type ColorFamily = '青瓷系' | '酱釉系' | '蓝釉系' | '白釉系' | '黑釉系' | '花釉系' | '结晶釉系';

// 光泽度
type Glossiness = '哑光' | '半哑光' | '半光泽' | '高光';

// 配方成分
interface RecipeIngredient {
  name: string;
  percentage: number;
}

// 釉料配方
interface GlazeRecipe {
  id: string;
  name: string;
  ingredients: RecipeIngredient[];
  notes?: string;
}

// 试片数据
interface TestSample {
  id: string;
  name: string;
  code: string;
  colorFamily: ColorFamily;
  hexColor: string;
  frontImage: string;
  backImage: string;
  firingTemperature: number;
  atmosphere: Atmosphere;
  clayType: string;
  glazeName: string;
  kilnPosition: string;
  firingDate: string;
  flowGrade: Grade;
  pinholeGrade: Grade;
  crazingGrade: Grade;
  glossiness: Glossiness;
  touchFeel: string;
  recipe: GlazeRecipe;
  evolutionChainId: string;
  version: number;
  previousVersionId?: string;
  nextVersionId?: string;
  notes?: string;
  suitableFor: string[];
}

// 颜色演变链路
interface EvolutionChain {
  id: string;
  name: string;
  description: string;
  sampleIds: string[];
}

// 筛选条件
interface FilterState {
  colorFamily: ColorFamily | '全部';
  temperatureRange: [number, number] | null;
  clayType: string | '全部';
  atmosphere: Atmosphere | '全部';
  kilnPosition: string | '全部';
  searchKeyword: string;
}
```
